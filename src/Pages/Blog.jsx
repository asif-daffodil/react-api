import { Helmet } from "react-helmet";

const Blog = () => {
    return (
        <div>
            <Helmet>
                <title>Blog</title>
            </Helmet>
            This is blog page
        </div>
    );
};

export default Blog;